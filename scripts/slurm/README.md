# SLURM

Store cluster job templates here.

Recommended split:

- one launcher per benchmark family
- one launcher per heavyweight hybrid experiment if needed
- environment setup scripts shared across jobs
