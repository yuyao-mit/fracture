# Metadata

This directory stores lightweight tracked metadata that makes runs reproducible.

- `splits/`: split manifests and dataset indices
- `runs/`: experiment registry, summarized metrics, and table exports small enough for git
