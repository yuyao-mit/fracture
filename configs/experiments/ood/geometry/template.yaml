name: fracture_ood_geometry_replace_me
benchmark: ood_geometry

paths:
  config: configs/paths/shared_paths.example.yaml

split:
  train_manifest: metadata/splits/geometry_train.json
  test_manifest: metadata/splits/geometry_ood_test.json

model:
  config: configs/models/operator_template.yaml

artifacts:
  run_subdir: ood/geometry/fracture_ood_geometry_replace_me
