# Experiment Configs

Each experiment config should combine:

- one path config
- one model config
- one solver config if applicable
- one split definition
- one output naming convention

Organize experiments by benchmark type:

- `id/`
- `ood/`
- `low_data/`
- `ablations/`
