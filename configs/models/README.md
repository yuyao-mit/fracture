# Model Configs

Use this directory for tracked model definitions.

- Add one config per neural operator in `src/models`.
- Add separate configs for hybrid NN+FEM variants.
- Keep naming aligned with stable run names.

Suggested convention:

- `operator_<name>.yaml`
- `hybrid_<name>.yaml`
