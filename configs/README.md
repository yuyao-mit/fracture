# Configs

This directory stores tracked configuration files only.

- `paths/`: data, checkpoint, and external artifact roots
- `models/`: neural-operator and hybrid model templates
- `solver/`: FEM and PDE solver settings
- `experiments/`: benchmark definitions for ID, OOD, low-data, and ablations

Do not place generated outputs here.
